#include<iostream>
#include<algorithm>
#include<string>
#include<math.h>
using namespace std;
int main()
{
    register int t;
    cin >> t;
    while(t--)
    {
        register int s,rev = 0;
        cin >> s;
        cout << (int)sqrt(s) << endl;
    }
    return 0;
}
